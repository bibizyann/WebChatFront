import React from 'react';
import {
    Card,
    CardContent,
} from "@/components/ui/card"

const CardComponent = () => {
    return (
        <Card>
            <CardContent>
                <p>Card Content</p>
            </CardContent>
        </Card>

    );
};

export default CardComponent;